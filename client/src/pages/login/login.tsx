import { Box } from '@mui/material';
import { PAGE_HEIGHT, PAGE_WIDTH } from '../../constants/constants';

function Login() {
  return <Box css={{ height: PAGE_HEIGHT, width: PAGE_WIDTH }}>Login Page</Box>;
}

export default Login;
